#pragma once

namespace shared_framework::runtime
{
// Shared promoted runtime messages should be declared here over time.
// This header is intentionally minimal until common systems are promoted.
}
